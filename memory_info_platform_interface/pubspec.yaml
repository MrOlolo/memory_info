name: memory_info_platform_interface
description: A common platform interface for memory_info.
version: 0.0.1
homepage: ''

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

  meta: ^1.3.0
  plugin_platform_interface: ^2.0.1


dev_dependencies:
  flutter_test:
    sdk: flutter